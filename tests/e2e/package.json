{
  "name": "lazyapps-e2e",
  "private": true,
  "scripts": {
    "install:lockfile": "pnpm install --ignore-workspace",
    "test": "playwright test"
  },
  "dependencies": {
    "@playwright/test": "1.58.2"
  }
}
